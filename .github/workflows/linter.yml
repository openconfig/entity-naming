name: Lint Code Base

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  lint:
    uses: openconfig/common-ci/.github/workflows/linter.yml@abb79c974d34c0ec6cda3bcfcb2edb6679d197e1
    permissions:
      contents: read
      packages: read
